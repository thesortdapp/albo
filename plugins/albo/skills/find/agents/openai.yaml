interface:
  display_name: "Find in Albo"
  short_description: "Search your saved Albo library"
  default_prompt: "Use $find to find something in my Albo library."
dependencies:
  tools:
    - type: "mcp"
      value: "albo"
      description: "Albo personal library"
      transport: "streamable_http"
      url: "https://mcp.albo.inc/mcp"
policy:
  allow_implicit_invocation: true
