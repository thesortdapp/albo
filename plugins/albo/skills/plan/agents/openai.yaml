interface:
  display_name: "Plan with Albo"
  short_description: "Plan with places and ideas you saved"
  default_prompt: "Use $plan to build a plan from my Albo saves."
dependencies:
  tools:
    - type: "mcp"
      value: "albo"
      description: "Albo personal library"
      transport: "streamable_http"
      url: "https://mcp.albo.inc/mcp"
policy:
  allow_implicit_invocation: true
