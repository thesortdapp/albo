interface:
  display_name: "Organize Albo"
  short_description: "Organize saves into Albo collections"
  default_prompt: "Use $organize to organize my Albo library."
dependencies:
  tools:
    - type: "mcp"
      value: "albo"
      description: "Albo personal library"
      transport: "streamable_http"
      url: "https://mcp.albo.inc/mcp"
policy:
  allow_implicit_invocation: true
